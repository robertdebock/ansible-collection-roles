---

- name: assert | Test if virtualbox_version is set correctly
  ansible.builtin.assert:
    that:
      - virtualbox_version is defined
      - virtualbox_version is string
      - virtualbox_version is not none
    quiet: yes
