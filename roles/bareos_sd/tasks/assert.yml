---

- name: assert | Test if bareos_sd_backup_configurations is set correctly
  ansible.builtin.assert:
    that:
      - bareos_sd_backup_configurations is defined
      - bareos_sd_backup_configurations is boolean
    quiet: yes

- name: assert | Test if bareos_sd_install_debug_packages is set correctly
  ansible.builtin.assert:
    that:
      - bareos_sd_install_debug_packages is defined
      - bareos_sd_install_debug_packages is boolean
    quiet: yes

- name: assert | Test if bareos_sd_hostname is set correctly
  ansible.builtin.assert:
    that:
      - bareos_sd_hostname is defined
      - bareos_sd_hostname is string
      - bareos_sd_hostname is not none
    quiet: yes

- name: assert | Test if bareos_sd_message is set correctly
  ansible.builtin.assert:
    that:
      - bareos_sd_message is defined
      - bareos_sd_message is string
      - bareos_sd_message is not none
    quiet: yes

- name: assert | Test if bareos_sd_max_concurrent_jobs is set correctly
  ansible.builtin.assert:
    that:
      - bareos_sd_max_concurrent_jobs is defined
      - bareos_sd_max_concurrent_jobs is number
    quiet: yes

- name: assert | Test if bareos_sd_tls_enable is set correctly
  ansible.builtin.assert:
    that:
      - bareos_sd_tls_enable is defined
      - bareos_sd_tls_enable is boolean
    quiet: yes

- name: assert | Test if bareos_sd_tls_verify_peer is set correctly
  ansible.builtin.assert:
    that:
      - bareos_sd_tls_verify_peer is defined
      - bareos_sd_tls_verify_peer is boolean
    quiet: yes

- name: assert | Test if bareos_sd_directors is set correctly
  ansible.builtin.assert:
    that:
      - bareos_sd_directors is defined
      - bareos_sd_directors is iterable
    quiet: yes

- name: assert | Test if bareos_sd_devices is set correctly
  ansible.builtin.assert:
    that:
      - bareos_sd_devices is defined
      - bareos_sd_devices is iterable
    quiet: yes

- name: assert | Test if bareos_sd_directors is set correctly
  ansible.builtin.assert:
    that:
      - bareos_sd_directors is defined
      - bareos_sd_directors is iterable
    quiet: yes

- name: assert | Test if bareos_sd_messages is set correctly
  ansible.builtin.assert:
    that:
      - bareos_sd_messages is defined
      - bareos_sd_messages is iterable
    quiet: yes

- name: assert | Test if bareos_sd_s3_profiles is set correctly
  ansible.builtin.assert:
    that:
      - bareos_sd_s3_profiles is defined
      - bareos_sd_s3_profiles is iterable
    quiet: yes
