---

- name: assert | Test if fips_policy is set correctly
  ansible.builtin.assert:
    that:
      - fips_policy is defined
      - fips_policy in [ "DEFAULT", "FUTURE", "FIPS", "LEGACY" ]
    quiet: yes
