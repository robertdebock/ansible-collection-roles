---
# tasks file for vault_configuration

# - name: include assert.yml
#   ansible.builtin.import_tasks: assert.yml
#   run_once: yes
#   delegate_to: localhost

# - name: include secret_engines.yml
#   ansible.builtin.import_tasks: secret_engines.yml

- name: include auth_engines.yml
  ansible.builtin.import_tasks: auth_engines.yml
  when:
    - vault_configuration_auth_engines is defined
