---

- name: assert | Test locale_lang
  ansible.builtin.assert:
    that:
      - locale_lang is defined
      - locale_lang is string
      - locale_lang is not none
    quiet: true

- name: assert | Test locale_language
  ansible.builtin.assert:
    that:
      - locale_language is defined
      - locale_language is string
      - locale_language is not none
    quiet: true

- name: assert | Test locale_lc_address
  ansible.builtin.assert:
    that:
      - locale_lc_address is defined
      - locale_lc_address is string
      - locale_lc_address is not none
    quiet: true

- name: assert | Test locale_lc_all
  ansible.builtin.assert:
    that:
      - locale_lc_all is defined
      - locale_lc_all is string
      - locale_lc_all is not none
    quiet: true

- name: assert | Test locale_lc_collate
  ansible.builtin.assert:
    that:
      - locale_lc_collate is defined
      - locale_lc_collate is string
      - locale_lc_collate is not none
    quiet: true

- name: assert | Test locale_lc_ctype
  ansible.builtin.assert:
    that:
      - locale_lc_ctype is defined
      - locale_lc_ctype is string
      - locale_lc_ctype is not none
    quiet: true

- name: assert | Test locale_lc_identification
  ansible.builtin.assert:
    that:
      - locale_lc_identification is defined
      - locale_lc_identification is string
      - locale_lc_identification is not none
    quiet: true

- name: assert | Test locale_lc_measurement
  ansible.builtin.assert:
    that:
      - locale_lc_measurement is defined
      - locale_lc_measurement is string
      - locale_lc_measurement is not none
    quiet: true

- name: assert | Test locale_lc_messages
  ansible.builtin.assert:
    that:
      - locale_lc_messages is defined
      - locale_lc_messages is string
      - locale_lc_messages is not none
    quiet: true

- name: assert | Test locale_lc_monetary
  ansible.builtin.assert:
    that:
      - locale_lc_monetary is defined
      - locale_lc_monetary is string
      - locale_lc_monetary is not none
    quiet: true

- name: assert | Test locale_lc_name
  ansible.builtin.assert:
    that:
      - locale_lc_name is defined
      - locale_lc_name is string
      - locale_lc_name is not none
    quiet: true

- name: assert | Test locale_lc_numeric
  ansible.builtin.assert:
    that:
      - locale_lc_numeric is defined
      - locale_lc_numeric is string
      - locale_lc_numeric is not none
    quiet: true

- name: assert | Test locale_lc_paper
  ansible.builtin.assert:
    that:
      - locale_lc_paper is defined
      - locale_lc_paper is string
      - locale_lc_paper is not none
    quiet: true

- name: assert | Test locale_lc_response
  ansible.builtin.assert:
    that:
      - locale_lc_response is defined
      - locale_lc_response is string
      - locale_lc_response is not none
    quiet: true

- name: assert | Test locale_lc_telephone
  ansible.builtin.assert:
    that:
      - locale_lc_telephone is defined
      - locale_lc_telephone is string
      - locale_lc_telephone is not none
    quiet: true

- name: assert | Test locale_lc_time
  ansible.builtin.assert:
    that:
      - locale_lc_time is defined
      - locale_lc_time is string
      - locale_lc_time is not none
    quiet: true

- name: assert | Test locale_timezone
  ansible.builtin.assert:
    that:
      - locale_timezone is defined
      - locale_timezone is string
      - locale_timezone is not none
    quiet: true
